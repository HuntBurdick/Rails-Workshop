<%= simple_form_for @post, :url => url_for(:controller => 'admin/posts', :action => 'update'), :method => 'put', :html => { :role => "form" } do |form| %>

	<%= render :partial => 'form', :object => form %>
	
	<%= submit_tag 'Submit Page', :class => 'btn btn-primary' %>

<% end %>