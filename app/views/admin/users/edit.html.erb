<%= simple_form_for @user, :url => url_for(:controller => 'admin/users', :action => 'edit'), :method => 'post', :html => { :role => "form" } do |form| %>

	<%= render :partial => 'form', :object => form %>
	
	<%= submit_tag 'Submit', :class => 'btn btn-primary' %>

<% end %>