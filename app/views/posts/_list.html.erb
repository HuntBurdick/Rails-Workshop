<% for post in @posts %>
	<div class="media">
	  <div class="media-body">
	    <h2 class="media-heading">
	    	<%= link_to post.title, post %>
	    </h2>
	    <%= redcloth(post.body) %>
	  </div>
	</div>
	<% unless post == @posts.last %>
		<hr />
	<% end %>
<% end %>