<%%= render :partial => 'admin/template/form_errors', :object => @child_item %>
<%%= simple_form_for @child_item, :url => url_for(:action => 'create'), :method => 'post', :html => { :role => "form" } do |form| %>

	<%%= render :partial => 'form', :object => form %>
	
	<%%= submit_tag 'Submit', :class => 'btn btn-primary' %>

<%% end %>